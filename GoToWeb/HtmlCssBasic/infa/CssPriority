
- если конфликт свойств при равноправном селекторе - выберется то свойство, которое написано позже
- приоритеты селекторов:
  более приоритетным считается тот селектор, которые потенциально на странице может выбрать меньшее количество элементов
  класс > имя_тега
  селектор   вес (приоритет)
  * -        0 (самый наименее приоритетный селектор)
  tag_name - 1
  .class -   10
  #id -      100
  [style] -  1000

  Ключевое слово !important - самый высокий приоритет(использовать не рекомендуют), например -  color: red !important;
  самый высокий приоритет имеет связка style+!important

  tag_name + .class > .class   =   11 > 10
  #id > .class .class   =  100 > 20